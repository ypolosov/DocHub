// Парсим манифест
// content - содержание манифеста
// loader - драйвер импорта подключаемых манифестов
export default function (content, loader) {
    // eslint-disable-next-line no-console
    console.info(content, loader);
}