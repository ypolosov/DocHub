const fs = require('fs');

module.exports = {
    devServer: {
        /*
        allowedHosts: [
            'dochub.rabota.space',
            'localhost'
        ],
        http2: true,
        https: {
            key: fs.readFileSync(__dirname + '/certs/server.key'),
            cert: fs.readFileSync(__dirname + '/certs/server.cert')
        }
        */
    }
};
